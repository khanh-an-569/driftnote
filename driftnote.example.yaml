# Copy this file to driftnote.yaml and keep the real file uncommitted.
vault_root: "D:/Notes/Second Brain"

# Optional: where generated Excalidraw diagrams are written, relative to vault_root.
# Defaults to the same folder as the source note when unset.
excalidraw_output_dir: "20 Knowledge/Excalidraw"

folders:
  inbox: "00 Inbox/Web"
  sources: "10 Sources"
  knowledge: "20 Knowledge"
  mocs: "30 MOCs"
  projects: "40 Projects"

capture:
  default_mode: "source-only"
  default_language: "vi"
  tavily: "fallback"
  min_content_chars: 400

privacy:
  public_web_only: true
  blocked_domains:
    - "mail.google.com"
    - "drive.google.com"
    - "localhost"
